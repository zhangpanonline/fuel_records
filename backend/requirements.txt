# Web 框架
fastapi==0.115.14
uvicorn[standard]==0.35.0

# 数据库
SQLAlchemy==2.0.41
PyMySQL==1.1.1
cryptography==43.0.3   # PyMySQL 连接 MySQL 8.0+ 需要

# 数据校验（FastAPI 依赖）
pydantic==2.11.7
pydantic-settings==2.7.1  # 读取 .env 配置

# 日志
loguru==0.7.3

# PostgreSQL 驱动（Render + Supabase 部署用）
psycopg2-binary==2.9.10

# 认证
bcrypt==4.2.1   # 密码哈希
PyJWT==2.10.1   # JWT 签发与验证

# 工具
python-dotenv==1.1.0
